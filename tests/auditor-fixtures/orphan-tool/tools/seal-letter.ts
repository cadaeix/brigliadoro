// Fixture file — read by the auditor as text. Stub factory; not executed.
// Wired in server.ts but missing from manifest.json — the orphan.

export function createSealLetter() {
  return { name: "seal_letter", description: "stub" };
}
